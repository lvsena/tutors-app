export class AcademicCourse {
  private name: string;
  private institution: string;

  constructor(name: string, institution: string) {
    this.name = name;
    this.institution = institution;
  }
}
